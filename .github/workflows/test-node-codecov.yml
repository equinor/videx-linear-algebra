name: Run Node Tests with Code Coverage

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch: {}

permissions: {}

jobs:
  generate-test-results:
    name: Generate Test Results
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Use Node.js 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24.x'

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Run tests with coverage
        id: test
        continue-on-error: true
        env:
          CI: true
        run: npm run coverage:ci

      - name: Strip standard Cobertura DOCTYPE
        if: ${{ always() }}
        run: sed -i '/<!DOCTYPE coverage SYSTEM "http://cobertura.sourceforge.net/xml/coverage-04.dtd">/d' coverage/Cobertura.xml 2>/dev/null || true

      - name: Upload coverage report
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ github.event_name == 'push' && 'coverage-master-baseline' || 'coverage' }}
          path: coverage/Cobertura.xml
          retention-days: ${{ github.event_name == 'push' && 90 || 5 }}
          if-no-files-found: warn

      - name: Upload test results
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-results
          path: test-results/junit.xml
          retention-days: 5
          if-no-files-found: warn

      - name: Fail job if tests failed
        if: ${{ always() && steps.test.outcome == 'failure' }}
        run: exit 1
